# Core dependencies
pybind11>=2.11.1
setuptools>=68.0.0
wheel>=0.41.0

# Build tools
cmake>=3.25.0
ninja>=1.11.0  # Optional but recommended for faster builds

# Python development tools
pytest>=7.4.0  # For testing the bindings
numpy>=1.24.0  # Required for numpy array support 