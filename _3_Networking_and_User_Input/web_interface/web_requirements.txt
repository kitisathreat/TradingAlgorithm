# =============================================================================
# Trading Algorithm Web Interface Requirements
# Optimized for faster installation with binary packages
# =============================================================================

# Core dependencies (binary packages)
streamlit==1.31.0
pandas==2.0.3
plotly==5.18.0
python-dotenv==1.0.0
requests==2.31.0
yfinance==0.2.63
numpy==1.24.3
tqdm==4.65.0

# GUI Application
PyQt6==6.6.1
PyQt6-Qt6==6.6.1
PyQt6-sip==13.6.0
pyqtgraph==0.13.3  # For high-performance plotting
qt-material==2.14  # For modern material design theme

# ML packages (binary distributions)
tensorflow-cpu==2.13.0  # Using CPU-only version for faster installation
scikit-learn==1.3.0
joblib==1.3.0
vaderSentiment==3.3.2

# Essential UI components only
streamlit-plotly-events==0.0.6
streamlit-option-menu==0.3.12

# API and data services (minimal set)
alpaca-trade-api==3.2.0
fastapi==0.100.0
websockets==13.0

# Remove unnecessary packages:
# - textblob (not essential for core functionality)
# - matplotlib (plotly is sufficient)
# - seaborn (not used in core functionality)
# - streamlit-extras (not essential)
# - streamlit-autorefresh (not essential)
# - opencv-python-headless (not needed for core functionality)
# - httpx and aiohttp (requests is sufficient)
# - tenacity (can be added if needed later) 