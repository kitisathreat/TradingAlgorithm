# Set the default behavior for all text files to have consistent line endings.
# This prevents issues when moving between Windows (CRLF) and Linux/macOS (LF).
* text=auto eol=lf

# --- Source Code ---
# Explicitly treat these as text with LF line endings.
*.h text eol=lf
*.cpp text eol=lf
*.py text eol=lf
*.js text eol=lf
*.css text eol=lf
*.html text eol=lf
*.sln text eol=lf
*.vcxproj text eol=lf
*.pyproj text eol=lf
*.md text eol=lf

# --- Configuration and Data ---
# Treat these as text as well.
.gitignore text eol=lf
.gitattributes text eol=lf
.env text eol=lf
*.csv text eol=lf

# --- Git LFS for Large Files ---
# Mark large binary files to be handled by Git Large File Storage (LFS).
# This keeps your main repository small and fast.
# NOTE: You must have Git LFS installed for this to work (git lfs install).
*.joblib filter=lfs diff=lfs merge=lfs -text